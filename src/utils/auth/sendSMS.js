async function sendSMS(phone, text) {
	// Here must be a function for sending sms with code to user
	// but it's not implemented
	console.log({
		phone,
		text,
	});

	return;
}

module.exports = sendSMS;
